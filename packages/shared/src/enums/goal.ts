export enum GoalType {
  RESULT = 'result',
  PROJECT = 'project',
  HABIT = 'habit',
}

export enum GoalLevel {
  VISION = 'vision',
  ANNUAL = 'annual',
  QUARTER = 'quarter',
  MONTH = 'month',
  WEEK = 'week',
  DAY = 'day',
}
